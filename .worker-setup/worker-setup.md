I want to create a worker definition based on .\.worker\worker-taxonomy-tree.md also referred to as worker-logic, this worker definition is going to hold our logic for how we work in this project from planning to execution to testing. The worker definition is a folder and document tree containing all sources, references and logic for how we build out application and logic.

A worker at any point needs to know what parts of the worker-logic it needs to load into memory to do a job succesfully.

To better understand what we are building you can study the information in .\.foundations and use that information to design and create the worker-logic we need.

The worker has a folder and file structure similar to the node tree in .\.worker\worker-taxonomy-tree.md, next to that it has a central command (file) i can refer to or add to memory when working on our application that contains the knowledge, entrypoints and navigation logic for our agents and devs to know how to work, behave and ship features e2e using the vision, rules, patterns, principles and techniques we need to make it work consistently to create high quality work and be able to review, test and self-learn along the way.

I want you to write an instruction for an llm agent who needs to set this up for building our app and add a checklist and start working on the instruction as well after that.

If you need any clarification to be able to create the worker-logic you can ask your questions 1-by-1 and also be sure to number all your questions and subquestions for better responses and if applicable show answering options or multiple choice.

Now lets setup the worker-logic in .\.worker.
